# Trust Level Avatar Flair - Compact SVG Set v4.2.1

Safe SVG build. Each badge is physically separated and tightly cropped. The standard SVG namespace and XML declaration are preserved so the files render as images when opened directly. No outer CSS shadow; min-width/min-height constraints are cleared.


## 4.2.2
- Topic-post badge height increased from 17px to 19px.
- Badge offset 5px outward and 4px vertically beyond the avatar edge.
- Avatar containers allow visible overflow; z-index raised to avoid clipping.
- User-card and profile sizes unchanged.
